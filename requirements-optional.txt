# QCM 可选依赖（按能力启用 · 缺失时自动降级，不阻断主流程）
# 对外发布版本 1.0.1 · 依赖说明见 DEPENDENCIES.md

websockets>=12.0        # WS 旁路推送 / WebSocket transport
graphql-core>=3.2       # GraphQL schema + subscription
opentelemetry-api>=1.20 # OTel 追踪
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp-proto-http>=1.20
opentelemetry-exporter-otlp-proto-grpc>=1.20
cryptography>=42.0      # Secret 加密（Fernet 优先）
prometheus-client>=0.20 # /metrics 指标

# 开发/测试（可选）
# pytest>=7.0
